# Runtime dependencies. Version ranges are upper-bounded on purpose: an
# unbounded ">=" let a routine `pip install` pull a fastapi/starlette
# combination that broke `APIRouter()` construction (see incident notes in
# DEPLOYMENT.md). Bump the upper bound deliberately after re-running the
# full test suite against the new versions.
fastapi>=0.128,<0.140
uvicorn[standard]>=0.34,<0.52
pydantic>=2.9,<2.14
python-dotenv>=1.0,<1.3
python-multipart>=0.0.20,<0.1
# Turkish morphological analyzer for optional CSV/JSON lemmatization
# (backend/services/lemmatizer.py) -- pure-Python, ~3MB + nltk dependency
# (~15MB), lazy-imported so it doesn't cost anything on requests that
# don't ask for it. See CLAUDE.md's lemmatization note for the measured
# cold-start cost (~2-3s one-time) that justifies the lazy loading.
zeyrek>=0.1,<0.2
